import { LoadingIndicatorInterceptor } from './loading-indicator-interceptor';

describe('LoadingIndicatorInterceptor', () => {
  it('should create an instance', () => {
    expect(new LoadingIndicatorInterceptor()).toBeTruthy();
  });
});
